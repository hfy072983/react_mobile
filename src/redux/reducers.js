// reducer:根据旧的prevState数据和action计算生成新的newState
import { combineReducers } from 'redux'

function xxx (prevState = {}, action) {
  switch (action.type) {


    default:
      return prevState
  }
}
export default combineReducers({
  xxx
})